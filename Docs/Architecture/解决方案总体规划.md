# 《神兽联盟》解决方案总体规划

版本：1.3.0｜日期：2026-09-21｜状态：本轮整合规划，不等于工程验收通过。

正式位置：DivineBeastsWorkspace/Docs/Architecture/解决方案总体规划.md（解决方案总体规划）。

本文件回答“工程如何组织、模块归谁、运行怎样联接、两期如何交付”；插件开发规范规定实现与验收；仓库级AGENTS.md（工程规则）规定AI与开发者的日常操作边界。三份文档使用同一版本与目录模型。附件的专业VFX细节保留在规范的专章中，不以简化目录替代原始设计。

历史实施依据见 [神兽联盟历史对话与插件工程实现参考](神兽联盟历史对话与插件工程实现参考.md)，原始定位见 [历史对话来源索引](../References/神兽联盟历史对话来源索引.md)。它们整理已读取的七个会话，不代替本规划，也不将历史助手建议视为已批准代码。ApplicationFlow 的本轮接口、实现边界及验证状态见 [插件中文入口](../../Game/Plugins/GameFoundation/Application/GamePlatformApplicationFlow/README.md)。

1. 来源、适用顺序与本次整合决定

本版由三类材料组成：来源直接采用是用户附件的VFX设计；既定约束沿用是本会话已经确定的工作空间、三角色服务器、三层复用、竞技五模块和Go业务边界；本次补充设计用于填补注册、上下文装配、异步生命周期和验证契约。后者不是附件原文已经给出的实现。

用户附件完整保存在来源原文，全文1547行。参考原文保留其原有命名和图示，不作为未经整合即可执行的目录创建指令。现行目录以本文件为准。

IN01｜根目录采用最新明确命名。 唯一根目录为 DivineBeastsWorkspace（神兽联盟工作空间）。只改变工作空间路径，不因此改变 DivineBeastsArena.uproject（主工程）、主模块名、资产挂载点、Go模块导入路径或已发布协议身份。实际本机目录迁移须单独核对脚本、外部工具和引用。

IN02｜MOBA玩法结构不被附件中的示意展开覆盖。 附件第3章把 MobaCore（MOBA核心示例）、MobaHero（英雄示例）、MobaAbility（技能示例）、MobaCombat（战斗示例）、MobaMatch（比赛示例）、MobaArena（竞技示例）展开成多个插件；此前定稿采用 GamePlatformArena（通用竞技插件）中的五个模块。此次按“整合VFX设计”处理：保持五模块，不创建上述六个示例玩法插件；明确新增 MobaPresentation（MOBA表现语义插件），只承载附件第5章的语义与适配。这是对旧规则“第二层仅一个插件”的限定扩展，不修改五个竞技模块身份。[U1：117—130、328—388]

IN03｜项目非VFX名称按既定工程保留。 附件中的 DivineBeastsCore（项目核心示例）映射到既有 DivineBeastsRuntime（项目运行时）；DivineBeastsHero（项目英雄示例）映射到 DivineBeastsCharacters（项目角色）；DivineBeastsAbility（项目技能示例）映射到 DivineBeastsAbilities（项目技能）；DivineBeastsMainArena（项目竞技示例）映射到 DivineBeastsArena（项目竞技插件）及现有主竞技场世界包。这里是设计职责映射，不是要求批量重命名已存在的代码、反射类或插件。[U1：132—145]

IN04｜VFX新增名称按附件直接采用。 保留 GamePlatformVFXClient（VFX客户端模块）、GamePlatformVFXEditor（VFX编辑器模块），不创建没有职责的运行时／服务器模块。新增 DivineBeastsPresentation（项目表现插件）及其双端中立／客户端两个模块。旧条件候选 DivineBeastsVFX（原项目特效策略候选）和 DivineBeastsSFX（原项目音效策略候选）中的“项目上下文、语义映射和目录注册”默认合入项目表现插件；这不合并VFX与SFX基础播放器，也不授权删除真实已有的特殊执行代码。[U1：460—523、1307—1331]

IN05｜公共VFX资源采用附件归属，避免双份。 新规划使用 DBAPresentationPack_Core（项目公共表现核心包）承载附件规定的公共VFX定义、目录和资源；不再新建旧候选 DBAVFXPack_Core（旧公共VFX包归属）。已有真实资产包先保留身份，按引用清单迁移或暂作别名，不能只改目录名。旧公共音效、动画和UI包继续各自拥有资源，不因名称“公共表现”就未经评审合成一个巨大包。[U1：529—578]

IN06｜解决附件“开放世界不依赖MOBA”与依赖图之间的张力。 附件第15章要求非MOBA世界绕过MOBA，第17章又画出项目表现直接依赖MOBA表现的示意。本次补充为：DivineBeastsPresentation（项目公共表现）不硬依赖 MobaPresentation（MOBA表现）；两者通过基础层中立请求／上下文扩展接口联接，由竞技专用组合根装配。运行时可以先MOBA语义适配、再项目上下文补充，但代码不因此形成MOBA反向导入项目，通用世界也不被迫启用竞技。[U1：923—962、1062—1102]

IN07｜保留机制、语义、内容三分工，同时补齐实际解析顺序。 附件图示有的先写Definition再写Data，有的末尾把Definition和Catalog并列；本版统一为：先由目录解析得到定义主资产ID，再由数据服务加载定义，然后读其软引用加载实际资产。目录已经预加载定义时可命中缓存，但不得另造一套VFX资产加载器。[U1：867—918、1140—1183]

IN08｜补齐目录完全排序与冲突结果。 附件给出四种来源优先级及匹配维度，未规定完整排序键。本版在插件规范P13中规定资格过滤、精确语义优先、同语义级别内容包优先、具体度和显式优先级，并给出同键冲突及父语义回退规则。这是本次新设计，不是UE内建解析算法。[U1：966—1040]

IN09｜完整保留10种行为与17类内容分类。 行为仍是平台运行抽象，内容分类仍是制作与核验标签，二者正交；不创建17个插件、17套运行类或10种Actor。专项客户端定义类按附件保留，实际实现可以共享组件和执行器，不制造平行引擎。[U1：732—863、1382—1485]

IN10｜只推广可复用的工程约束，不臆造其他领域细则。 SFX（音效）、UI（用户界面）、纯表现动画和相机沿用语义入口、作用域服务、数据加载、内容所有权和验证思想；它们自己的行为分类、参数结构和性能预算不能照抄VFX十类型，须由领域设计定义。权威动作继续走动作执行链，而非可丢弃的特效请求链。

IN11｜材料未提供的项目明确待验证。 附件没有真实引擎构建结果、各目标设备的CPU／GPU／内存数值预算、全部已编译接口签名或真实资产列表。本版不填入虚假的性能阈值、不宣称已有实现完成；新增的接口、配置字段和检查项均作为待工程实现的设计约束。

2. 产品、技术与服务器基线

游戏采用 UE5.8（虚幻引擎5.8）C++；实时权威由 Dedicated Server（专用服务器）承载；长期业务由 Go（Go语言）后端承载。引擎补丁、编译器、Go、协议生成器及依赖以真实仓库锁定文件为准，不在文档中自行指定“最新版本”，不为通过测试降低生产版本。

唯一正式游戏工程是 Game/DivineBeastsArena.uproject（神兽联盟主工程）。主工程保持薄装配；正式客户端、服务器、编辑器分别构建。保留 DivineBeastsArenaServer.Target.cs（三角色共用服务器构建目标），默认不增加三个角色目标，更不为每种竞技人数创建单独二进制程序。独立资源烘焙配置、部署配置和进程实例不等于额外Target（构建目标）。

正式服务器角色只有三种：

OpenWorld  # 常驻世界：登录大厅＋主城＋开放世界区域
Village    # 新手村：正常新手村＋教学＋训练
MainArena  # 短生命周期竞技实例：1v1、2v2、3v3、4v4、5v5

Lobby（大厅）可作为常驻世界的体验、界面和区域名称，但不能恢复为独立服务器角色、独立资源池、独立项目世界插件或独立世界包。Village.Main（主新手村）、Village.Tutorial（教学）、Village.Training（训练）是体验配置。常驻世界可以有多个区域、实例和分片，并不意味着所有玩家共用同一个进程。

竞技采用两支比赛队伍，单队人数为1至5，总人数分别为2、4、6、8、10。人数由 ArenaModeDefinition（竞技模式定义）读取，不从已有协议枚举值推算。Party（赛前组队）不可拆分，但不是所有玩家的必经步骤；MatchTeam（局内比赛队伍）与赛前组队分开建模。

沿用此前已取消的玩法约束：不得复活五大阵营、旧派系、旧五行克制／共鸣、旧王印等规则。MOBA队伍敌我关系和美术素材中的普通词汇不构成恢复旧系统，不能按字符串机械删除第三方代码。

3. 责任边界与三层复用

**基础层 GameFoundation（游戏平台基础层）**提供跨游戏机制：身份、数据、流程、在线通信、会话、世界、通用角色／技能／战斗、表现框架和服务器控制面。它不知道生肖、项目技能或项目地图。

**类型层 MobaCommon（MOBA通用层）**在通用机制上组织竞技模式、队伍、比赛、计分与胜负；新增的表现插件只说明MOBA发生了什么。它不实现第二套Niagara（粒子特效）播放器，也不知道任何生肖资源路径。

**项目层 DivineBeasts（神兽联盟项目层）**提供稳定项目身份、真实流程、必要规则扩展、项目上下文、默认映射和内容包。ContentPacks（内容包）是这一层内部的交付分类，不是第四架构层。

本文件所有依赖图中 A → B只表示“A依赖B”；业务流程图另行标注为执行顺序。允许项目依赖类型层／基础层，类型层依赖基础层；禁止反向依赖和同层循环。三层不机械映射成三层类继承或三个同名子系统。

运行所有权另行划分。 Go后端拥有账号、角色档案、权益、订单、账本、业务组队、匹配和全局实例分配；UE服务器拥有世界内移动、命中、碰撞、技能和比赛事实；客户端拥有输入、经设计的预测和表现。子系统是服务生命周期机制，不能代替网络复制状态和实体组件。

4. 应用流程与两条世界路径

以下为执行顺序，不是代码依赖链：

Boot（启动）→ Initialize（初始化）→ Login（登录）
  → LoadPlayerProfile（加载玩家资料）
  → CharacterEntry（创建新角色／选择已有角色）
  → ValidateSelection（后端校验角色、英雄资格和选择版本）
  → RequestWorldAssignment（请求世界分配与一次性准入票据）
  ├─ 常规入口：OpenWorld（常驻世界）的登录大厅／主城安全落点
  └─ 新手入口：Village（新手村）主体验／教学／训练 → 完成后进入常驻世界
  → WorldReady（客户端必要资源＋服务器准入＋世界就绪）
  → 移动、交互、探索、可选组队
  → 选择1v1～5v5模式 → 匹配 → MainArena（主竞技场）
  → 权威比赛 → 幂等结果提交 → 结算显示
  → 后端重新确认返回实例／容量／版本／安全落点 → 返回常驻世界

应用流程不亲自访问数据库、计算伤害、创建特效或操作具体控件；使用服务契约和项目节点。UI（用户界面）提供操作入口，后端决定角色与权益是否合法。创建一个持久角色档案、选择一个已有角色、取得英雄权益和对局选人是不同业务，不得共用一个模糊的“HeroId即可创建一切”接口。

组队和匹配状态可与玩家在常驻世界中活动并存，不应作为世界状态的互斥替代。同实例的大堂、主城、野外采用区域流送或世界内传送；跨实例、跨分片、新手村和竞技场则走真实迁移与准入。比赛结束或断线后不能直接信任旧地址或重用已消费票据。

5. 完整工作空间与插件目录

这是一份完整目标归属树，不是扫描所得的现有工程清单。所有插件按“一期必建／条件拆出／随内容交付／二期预留”区分。条件拆出不意味着功能延期；只有形成独立职责、测试或发布边界才建立独立插件，不能批量生成空描述文件和伪可用接口。

分类文件夹不创建描述文件；真实插件拥有唯一同名 .uplugin（插件描述文件）。同一插件可有多个不同名模块。插件发现和模块组织由UE原生机制支持，但本项目三层分类是工程约定而不是引擎强制层级。[S01][S02]

DivineBeastsWorkspace/                                     # 神兽联盟唯一工作空间根目录
├── AGENTS.md                                              # AI／开发者强制规则；本次交付
├── .gitignore                                             # 源码与缓存边界，不删除地图外置编辑数据
├── Game/                                                  # UE5.8唯一正式游戏工程；薄宿主＋三层插件
│   ├── DivineBeastsArena.uproject                         # 正式游戏项目与最终装配
│   ├── Source/                                            # 主模块、启动组合与构建目标
│   │   ├── DivineBeastsArena/                             # 薄主模块；不是项目竞技运行模块
│   │   ├── DivineBeastsArenaClient.Target.cs              # 客户端目标
│   │   ├── DivineBeastsArenaServer.Target.cs              # 三类服务器共用目标
│   │   └── DivineBeastsArenaEditor.Target.cs              # 编辑器与烘焙目标
│   ├── Plugins/                                           # UE唯一插件源码根目录；分类目录不是插件
│   │   ├── README.md                                      # 插件文档兼容入口；正文见插件开发规范.md
│   │   ├── 插件开发规范.md                                # 插件开发规范V1.3.0；唯一正式规范正文
│   │   ├── GameFoundation/                                # 第一层：跨游戏通用机制
│   │   │   ├── Core/                                      # 核心基础分类
│   │   │   │   ├── GamePlatformCore/                      # [一期必建] 中立身份、结果、版本、日志与基础契约
│   │   │   │   └── GamePlatformData/                      # [一期必建] 定义基类、资产身份、统一加载、租约与校验
│   │   │   ├── Application/                               # 应用框架与跨地图服务
│   │   │   │   ├── GamePlatformApplicationFlow/           # [一期必建] 流程执行机制；具体节点由组合根注入
│   │   │   │   ├── GamePlatformOnline/                    # [一期必建] 平台公共认证、传输与业务适配
│   │   │   │   ├── GamePlatformSession/                   # [一期必建] 玩家会话、准入票据携带、切服与重连
│   │   │   │   ├── GamePlatformLoading/                   # [一期必建] 资源预加载和世界可玩就绪屏障
│   │   │   │   ├── GamePlatformSettings/                  # [二期预留] 独立设置扩展；一期保留必要基础设置
│   │   │   │   ├── GamePlatformSave/                      # [二期预留] 非权威本地数据存储
│   │   │   │   ├── GamePlatformLocalization/              # [二期预留] 独立多语言与地区格式扩展
│   │   │   │   ├── GamePlatformTelemetry/                 # [二期预留] 独立遥测上传；一期日志诊断不延期
│   │   │   │   └── GamePlatformLiveOps/                   # [二期预留] 通用赛季、活动与运营配置接入
│   │   │   ├── Gameplay/                                  # 通用世界、角色与玩法机制
│   │   │   │   ├── GamePlatformWorld/                     # [一期必建] 世界生命周期、区域登记与流送协调
│   │   │   │   ├── GamePlatformOpenWorld/                 # [条件拆出] 独立区域活动策略；三维世界能力仍在一期
│   │   │   │   ├── GamePlatformPCG/                       # [一期必建] 程序化生成接入；优先编辑器生成和验证
│   │   │   │   ├── GamePlatformInput/                     # [一期必建] 本地玩家输入语义、设备与输入上下文
│   │   │   │   ├── GamePlatformGameplay/                  # [一期必建] 体验、加入、出生与规则装配扩展点
│   │   │   │   ├── GamePlatformCharacter/                 # [一期必建] 通用角色实体、移动与能力组件
│   │   │   │   ├── GamePlatformAbilitySystem/             # [一期必建] GAS技能、属性、效果、冷却与必要动作任务
│   │   │   │   ├── GamePlatformCombat/                    # [一期必建] 命中、伤害、治疗、控制及权威战斗事实
│   │   │   │   ├── GamePlatformInteraction/               # [一期必建] 拾取、采集、场景交互与权威校验
│   │   │   │   ├── GamePlatformAI/                        # [二期预留] 独立人工智能框架
│   │   │   │   ├── GamePlatformNavigation/                # [二期预留] 独立导航扩展
│   │   │   │   ├── GamePlatformQuest/                     # [二期预留] 任务、教学进度与活动目标
│   │   │   │   ├── GamePlatformInventory/                 # [二期预留] 背包视图、快捷栏与道具请求
│   │   │   │   ├── GamePlatformEntitlement/               # [二期预留] 权益读取与解锁表现
│   │   │   │   ├── GamePlatformEquipment/                 # [二期预留] 装备槽、挂点与外观装配
│   │   │   │   └── GamePlatformProgression/               # [二期预留] 长期等级、经验与成长展示
│   │   │   ├── Presentation/                              # 通用表现契约和各领域执行器
│   │   │   │   ├── GamePlatformPresentation/              # [一期必建] 中立请求、上下文扩展、提供者注册与协调
│   │   │   │   │   └── Source/                            # 中立契约与客户端协调分离
│   │   │   │   │       ├── GamePlatformPresentationCore/  # 双端中立契约；保留既有模块名
│   │   │   │   │       └── GamePlatformPresentationClient/ # 客户端协调与上下文扩展点；不反向依赖播放器
│   │   │   │   ├── GamePlatformUI/                        # [一期必建] 本地玩家页面栈、界面目录与视图模型
│   │   │   │   ├── GamePlatformAnimation/                 # [一期必建] 双端必要动作与客户端纯表现动画分离
│   │   │   │   ├── GamePlatformVFX/                       # [一期必建] 唯一通用视觉特效运行框架
│   │   │   │   │   ├── GamePlatformVFX.uplugin            # V0.1.0唯一描述文件；客户端模块排除Server目标
│   │   │   │   │   ├── README.md                          # 中文职责、接入限制与首条验证链
│   │   │   │   │   ├── Config/                            # VFX默认配置；实际加载机制待UE验证
│   │   │   │   │   ├── Content/                           # VFX资产分类与占位目录；真实.uasset仅由编辑器创建
│   │   │   │   │   ├── Docs/                              # 架构、定义、目录、生命周期、依赖和验证中文说明
│   │   │   │   │   ├── Source/                            # V0.1.0实际C++源码
│   │   │   │   │       ├── GamePlatformVFXClient/         # 客户端正式运行；含定义、目录、执行与私有世界子系统
│   │   │   │   │       └── GamePlatformVFXEditor/         # 编辑器验证、详情面板与人工核验工具
│   │   │   │   │   └── Tests/                             # Data Validation、Cook、Server Stage审计脚本及夹具
│   │   │   │   ├── GamePlatformSFX/                       # [一期必建] 独立音效运行框架与混音接入
│   │   │   │   ├── GamePlatformCamera/                    # [一期必建] 本地相机模式与表现反馈
│   │   │   │   └── GamePlatformCommerceUI/                # [二期预留] 商品、订单、充值与支付结果界面
│   │   │   ├── Server/                                    # 通用服务器控制面
│   │   │   │   └── GamePlatformServer/                    # [一期必建] 三角色共用注册、心跳、世界就绪和准入
│   │   │   └── Developer/                                 # 开发工具分类
│   │   │       ├── GamePlatformDebug/                     # [条件拆出] 独立调试面板；基础诊断随能力交付
│   │   │       └── GamePlatformDeveloperTools/            # [条件拆出] 跨插件工具；领域校验先由各编辑器模块提供
│   │   ├── MobaCommon/                                    # 第二层：MOBA竞技规则与中立表现语义
│   │   │   ├── GamePlatformArena/                         # [一期必建] 保留既定竞技插件和五模块
│   │   │   │   ├── GamePlatformArena.uplugin              # 竞技插件描述文件
│   │   │   │   └── Source/                                # 以下五项是模块，不是独立插件
│   │   │   │       ├── GamePlatformMobaCore/              # MOBA事实、规则契约与稳定身份
│   │   │   │       ├── GamePlatformMobaData/              # 竞技数据定义，通过英雄ID关联通用角色
│   │   │   │       ├── GamePlatformArena/                 # 双端比赛状态、复制模型与通用竞技机制
│   │   │   │       ├── GamePlatformArenaClient/           # 客户端竞技流程、选人、界面和结果适配
│   │   │   │       └── GamePlatformArenaServer/           # 服务器比赛推进、胜负和结果提交
│   │   │   └── Presentation/                              # 新增表现语义分类；不替代竞技五模块
│   │   │       └── MobaPresentation/                      # [一期必建] MOBA事实到中立表现请求的适配；不播放Niagara
│   │   │           ├── MobaPresentation.uplugin           # MOBA表现插件描述
│   │   │           └── Source/                            # 中立语义与客户端适配分离
│   │   │               ├── MobaPresentationRuntime/       # 共享语义、标签、上下文；不让竞技底座反向依赖它
│   │   │               └── MobaPresentationClient/        # 读取竞技事实并提交中立请求；调用项目上下文扩展接口
│   │   └── DivineBeasts/                                  # 第三层：神兽联盟项目配置、必要扩展和内容
│   │       ├── Core/                                      # 轻量项目标识和必要结构
│   │       │   ├── DivineBeastsRuntime/                   # [一期必建] 项目稳定ID和中立类型；不改名为DivineBeastsCore
│   │       │   └── DivineBeastsData/                      # [条件拆出] 真正跨功能共用的项目数据差异
│   │       ├── Application/                               # 项目流程装配
│   │       │   └── DivineBeastsApplicationFlow/           # [一期必建] 登录、创建／选择英雄、世界准入与可选竞技节点
│   │       ├── Gameplay/                                  # 项目玩法扩展
│   │       │   ├── DivineBeastsCharacters/                # [一期必建] 十二生肖角色规则与组件；保留复数既有名称
│   │       │   ├── DivineBeastsAbilities/                 # [条件拆出] 数据及通用任务不能表达的项目技能
│   │       │   ├── DivineBeastsCombat/                    # [条件拆出] 确有差异的项目伤害或控制策略
│   │       │   └── DivineBeastsArena/                     # [一期必建] 1v1～5v5项目配置；C++模块不得与主工程重名
│   │       ├── World/                                     # 世界行为与项目生成规则
│   │       │   ├── DivineBeastsOpenWorld/                 # [条件拆出] 登录大厅＋主城＋野外统一行为归属
│   │       │   ├── DivineBeastsVillage/                   # [条件拆出] 新手村／教学／训练独立行为
│   │       │   └── DivineBeastsPCG/                       # [随内容交付] 项目可复用生态规则与生成资产
│   │       ├── Presentation/                              # 项目表现上下文与必要适配
│   │       │   ├── DivineBeastsPresentation/              # [一期必建] 项目上下文、默认目录及内容包装配；不硬依赖MOBA
│   │       │   │   ├── DivineBeastsPresentation.uplugin   # 项目表现插件描述文件
│   │       │   │   └── Source/                            # 沿用附件名称；通过中立接口装配MOBA扩展
│   │       │   │       ├── DivineBeastsPresentationRuntime/ # 项目身份、上下文和中立表现事件
│   │       │   │       └── DivineBeastsPresentationClient/ # 上下文补充、默认映射与内容注册协调
│   │       │   ├── DivineBeastsUI/                        # [一期必建] 项目页面和视图模型；不由通用表现协调代替
│   │       │   ├── DivineBeastsAnimation/                 # [条件拆出] 确需专门的动作适配；资源差异进入内容包
│   │       │   └── DivineBeastsCamera/                    # [条件拆出] 确需专门相机行为；一般差异通过定义表达
│   │       ├── Server/                                    # 项目专用服务器装配
│   │       │   └── DivineBeastsServer/                    # [一期必建] 常驻世界、新手村、主竞技场的项目控制面
│   │       └── ContentPacks/                              # 第三层内部内容包分类，不是第四层
│   │           ├── Common/                                # 跨包真正共用的非领域资产
│   │           │   └── DBAContentPack_Common/             # [随内容交付] 共用骨架、材质与基础环境；不反向依赖英雄
│   │           ├── Heroes/                                # 十二生肖英雄，逐个制作和验证
│   │           │   ├── DBAHeroPack_Rat/                   # [随内容交付] 子鼠·影牙玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Ox/                    # [随内容交付] 丑牛·玄角玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Tiger/                 # [随内容交付] 寅虎·白君玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Rabbit/                # [随内容交付] 卯兔·玉灵玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Dragon/                # [随内容交付] 辰龙·苍龙玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Snake/                 # [随内容交付] 巳蛇·幽鳞玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Horse/                 # [随内容交付] 午马·雷蹄玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Goat/                  # [随内容交付] 未羊·玉角玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Monkey/                # [随内容交付] 申猴·灵猴玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Rooster/               # [随内容交付] 酉鸡·金鸣玩法与专属表现内容
│   │           │   ├── DBAHeroPack_Dog/                   # [随内容交付] 戌狗·天犬玩法与专属表现内容
│   │           │   └── DBAHeroPack_Pig/                   # [随内容交付] 亥猪·玄鬃玩法与专属表现内容
│   │           ├── Worlds/                                # 三个服务器角色对应世界内容归属
│   │           │   ├── DBAWorldPack_OpenWorld/            # [随内容交付] 常驻世界地图、大厅／主城／野外、环境VFX
│   │           │   ├── DBAWorldPack_Village/              # [随内容交付] 新手村、教学和训练地图与表现
│   │           │   └── DBAWorldPack_MainArena/            # [随内容交付] 1v1～5v5共用竞技地图与表现
│   │           ├── Presentation/                          # 项目公共表现资源，实际资源只有一个所有者
│   │           │   ├── DBAPresentationPack_Core/          # [随内容交付] 按附件承载公共VFX；替代旧规划DBAVFXPack_Core归属
│   │           │   ├── DBASFXPack_Core/                   # [随内容交付] 保留独立公共音效包；不未经评审合并
│   │           │   ├── DBAAnimationPack_Core/             # [随内容交付] 保留独立公共动画包
│   │           │   └── DBAUIPack_Core/                    # [随内容交付] 保留独立公共界面资产包
│   │           └── Optional/                              # 二期可选内容分类；不用空插件占位
│   │               ├── Skins/                             # 皮肤内容包
│   │               ├── Seasons/                           # 赛季内容包
│   │               └── Events/                            # 活动内容包
│   ├── Content/                                           # 最终组合资产，不堆放可复用框架
│   │   ├── Bootstrap/                                     # 启动内容
│   │   ├── Composition/                                   # 项目内容清单和功能组合
│   │   └── Development/                                   # 项目测试资产；单独烘焙排除
│   ├── Config/                                            # 项目最终配置、资产扫描和目标覆盖
│   ├── Build/                                             # UE自身图标、平台等构建资源
│   └── Tests/                                             # 项目测试材料；C++测试归实际模块
├── Shared/                                                # UE与Go共享层；不是UE业务插件库
│   ├── Contracts/                                         # 跨语言唯一协议真源
│   │   ├── GamePlatform/                                  # 平台公共契约
│   │   │   ├── OpenAPI/                                   # HTTP接口契约
│   │   │   ├── Proto/                                     # 跨语言消息和远程调用
│   │   │   ├── Schemas/                                   # 数据结构约束
│   │   │   └── Events/                                    # 真实跨UE和后端的事件
│   │   └── Games/                                         # 项目扩展契约
│   │       └── DivineBeasts/                              # 神兽联盟项目契约
│   │           ├── OpenAPI/                               # HTTP接口契约
│   │           ├── Proto/                                 # 跨语言消息和远程调用
│   │           ├── Schemas/                               # 数据结构约束
│   │           └── Events/                                # 真实跨UE和后端的事件
│   ├── Generated/                                         # 共享生成产物，不人工编辑
│   │   └── Cpp/                                           # 唯一C++生成源码；由统一构建编译
│   └── Docs/                                              # 共享协议权限、版本和生成映射
├── Backend/                                               # Go业务后端；本次不重排真实已批准内部布局
│   ├── go.mod                                             # 保留既有Go模块导入身份与锁定版本
│   ├── cmd/                                               # 五个薄应用入口
│   │   ├── gatewayservice/                                # 统一接入
│   │   ├── identityservice/                               # 身份认证
│   │   ├── playerdataservice/                             # 长期玩家数据
│   │   ├── matchservice/                                  # 组队、匹配、比赛编排
│   │   └── gameservercontrolservice/                      # 服务器分配、跨服与控制
│   ├── internal/                                          # 领域、编排、适配；以当前真实源码为准
│   │   └── contracts/                                     # 仅后端内部协议／事件；不得导出到UE
│   ├── pkg/                                               # 真正可复用Go公共包
│   ├── generated/                                         # Go共享及内部协议产物，分类但不重复生成
│   ├── migrations/                                        # 版本化数据库迁移
│   ├── configs/                                           # 非敏感配置
│   └── tests/                                             # 后端测试
├── Build/                                                 # 工作空间统一构建
│   ├── Game/                                              # 编辑器／客户端／服务器构建
│   ├── Backend/                                           # Go后端构建与检查
│   ├── Contracts/                                         # 契约生成、兼容检查和外部库构建
│   ├── Validation/                                        # 代码、资产、规则与产物门禁
│   └── Packaging/                                         # 发布打包、许可和摘要
├── Deploy/                                                # 唯一部署目录
│   ├── Local/                                             # 本地联调
│   │   ├── StartBackendDevelopment.ps1                    # 后端五服务构建、启动与就绪检查入口
│   │   └── StopBackendDevelopment.ps1                     # 后端本地容器停止入口；不删除卷或镜像
│   ├── Docker/                                            # 镜像构建
│   │   ├── Backend.Dockerfile                             # 单服务 Go 静态镜像构建定义
│   │   └── docker-compose.backend.local.yml               # 五服务本地开发 Compose 拓扑
│   ├── Server/                                            # 三角色部署配置
│   │   ├── OpenWorld/                                     # 常驻世界角色配置，不是额外构建目标
│   │   ├── Village/                                       # 新手村＋教学＋训练配置
│   │   └── MainArena/                                     # 短生命周期1v1～5v5配置
│   └── Production/                                        # 正式部署与回退
├── Tests/                                                 # 跨系统验证
│   ├── Deployment/                                        # 部署结构与运行态验证
│   │   ├── ValidateBackendDevelopmentDeployment.ps1       # 后端 Compose 服务、构建和内部地址校验
│   │   └── StartBackendDevelopmentIntegration.ps1         # 一键启动及五服务 HTTP 就绪集成校验
│   ├── Integration/                                       # 跨插件及协议集成
│   ├── Network/                                           # 准入、复制、预测、重连
│   ├── Performance/                                       # 目标平台性能预算
│   └── E2E/                                               # 真实世界和五模式端到端
├── Tools/                                                 # 辅助工具
│   ├── CodeGen/                                           # 代码生成工具
│   ├── AssetTools/                                        # 引擎驱动的资产处理
│   ├── DataTools/                                         # 数据检查
│   └── DevHosts/                                          # 既有薄测试宿主归属；共享唯一插件源码
│       ├── GamePlatformPresentationDev/                   # 平台表现核验，无MOBA和生肖依赖
│       ├── GamePlatformRuntimeClientDev/                  # 通用世界／角色／准入测试
│       ├── GamePlatformMobaClientDev/                     # 中立竞技客户端测试；已批准宿主才实体化
│       └── GamePlatformMobaServerDev/                     # 中立竞技专用服务器测试
└── Docs/                                                  # 正式文档和可追溯参考
    ├── README.md                                          # 工程文档总入口；合入时保留用户原文
    ├── CHANGELOG.md                                       # 版本变更；本次文档记录不替代历史
    ├── Architecture/                                      # 总体、依赖和目录规划
    │   ├── 解决方案总体规划.md                            # 现行总体架构、整合决定与两期交付规划
    │   ├── 解决方案总体目录规划说明_V1.3.0.md              # 现行目录规划；目录变更必须同步维护
    │   └── SolutionDirectoryTree_CN_V1.1.0.md             # 历史目录规划索引；仅保留版本追溯
    ├── Gameplay/                                          # 玩法与体验设计
    ├── Networking/                                        # 权威、复制与连接
    ├── Backend/                                           # 后端和数据所有权
    ├── Art/                                               # 角色、美术和VFX制作规范
    ├── Production/                                        # 交付与验证证据
    │   ├── 业务后端本地部署说明.md                         # 后端 Docker 本地部署、验证、排障和停止说明
    │   └── 业务后端与共享代码工程化审查报告_V1.3.0.md      # Backend／Shared静态审查、阻断项与验证链
    └── References/                                        # 来源原文，非现行工程指令

“目录规范存在”不等于实际目录应全部建立。现有后端内部布局、普通游戏目标和已批准开发宿主先核查再沿用；这里没有列出细节不表示应删除。插件的物理分类路径也不是资产挂载路径；实际挂载点通过引擎／插件注册获取。

6. MOBA竞技五模块与新表现插件的协作

竞技插件内五模块继续为 GamePlatformMobaCore（MOBA契约）、GamePlatformMobaData（MOBA数据）、GamePlatformArena（双端竞技）、GamePlatformArenaClient（竞技客户端）、GamePlatformArenaServer（竞技服务器）。

GamePlatformArenaClient（竞技客户端） → GamePlatformArena（双端竞技）
GamePlatformArenaServer（竞技服务器） → GamePlatformArena（双端竞技）
GamePlatformArena（双端竞技） → GamePlatformMobaData（MOBA数据）
GamePlatformMobaData（MOBA数据） → GamePlatformMobaCore（MOBA契约）
GamePlatformMobaData（MOBA数据） → GamePlatformData（平台数据）
GamePlatformMobaCore（MOBA契约） → GamePlatformCore（平台核心）

竞技运行可以使用通用玩法／战斗；数据不能反向依赖运行类型。通用英雄定义归平台角色，竞技参数经英雄ID关联；关闭MOBA仍能运行基础角色与常驻世界。

新增 MobaPresentation（MOBA表现语义插件）包含附件指定的两个模块。MobaPresentationRuntime（共享语义模块）只持有中立标签、上下文和事件，不持有资源；MobaPresentationClient（客户端适配模块）读取竞技已复制事实并产生中立表现请求。依赖是表现适配指向竞技事实，不是竞技权威规则指向表现插件。 竞技五模块不反向链接该插件，避免模块图看似无环、插件图却成环。

玩法事实的标签／载荷由原有事实拥有者定义，表现语义由表现模块定义，同一稳定标签只有一个声明所有者。必要双端表现语义模块可以由产品组合按目标启用，但服务器不因其可见而播放特效。也不能把C++局部委托广播当作网络传输。

7. 统一表现架构：VFX的正式整合

来源直接采用：基础层负责“怎么运行”，类型层负责“发生了什么”，项目层负责“最终显示成什么样”。[U1：5—46]

平台中立入口。 GamePlatformPresentationCore（表现核心模块）保留既有名称，定义轻量请求、结果、上下文扩展和服务契约；GamePlatformPresentationClient（表现协调模块）负责客户端作用域注册与调度。协调模块不反向导入具体VFX、SFX或UI播放器。真实播放器向中立扩展点注册，作用域关闭时撤销。

唯一VFX运行框架。 GamePlatformVFX（视觉特效插件）一期只含客户端和编辑器模块。客户端拥有服务接口、定义类型、目录类型、设置、私有世界子系统、提供者、解析器、执行器、实例登记、预加载协调、池化策略、质量策略、复合编排和诊断。V0.1.0 源码、中文说明、测试脚本和内容目录已接入 `Game/Plugins/GameFoundation/Presentation/GamePlatformVFX/`；它仍是生产纵向切片，当前只完成文件一致性核对，尚无 UE5.8 编译、Cook、Stage 或真实 `GamePlatformData`／`GamePlatformPresentation` 对接通过的证据。完整目录与验收要求见插件规范P08。[U1：161—322、1307—1331]

项目公共表现。 DivineBeastsPresentation（项目表现插件）提供项目身份上下文、默认目录及内容注册协调；不囤积大量英雄美术。它实现基础层中立上下文扩展接口，竞技专用组合根将其与MOBA适配联接。常驻世界直接使用项目表现或中立表现，无须安装MOBA。

竞技表现执行顺序：

服务器权威技能／战斗／比赛事实
  → GameplayCue（GAS表现通知）或已复制事实／受控网络消息
  → 客户端有效性、可见性、时效与预测去重检查
  → MobaPresentationClient（MOBA语义适配）
  → 通过中立扩展接口补充项目上下文
  → GamePlatformPresentationClient（客户端表现协调）
  → GamePlatformVFXPresentationProvider（VFX提供者）
  → GamePlatformVFXWorldSubsystem（私有世界级运行服务）
  → Catalog＋Resolver（目录与解析器）
  → Definition PrimaryAssetId（定义主资产ID）
  → GamePlatformData（数据服务）加载定义与所需资源
  → Niagara Executor（粒子执行器）
  → Instance Handle（可停止、更新和释放的实例句柄）

世界表现执行顺序：常驻世界／新手村事实 → 项目上下文扩展 → 中立表现服务 → VFX运行框架。以上箭头是调用／数据流；不表示MOBA插件依赖项目插件，也不表示项目公共表现必须导入MOBA。

低层 IGamePlatformVFXService（视觉特效服务接口）只给客户端集成、专门制作工具和测试使用；权威Gameplay（玩法）不能因接口“看起来很抽象”而链接VFX客户端模块。玩法正常入口仍是中立表现契约。

8. VFX行为、内容分类与资源归属

以下十种行为原样保留其含义，全部由唯一平台VFX框架实现：

01 Instant（瞬时型）
02 Attached（附着型）
03 Projectile（视觉投射型）
04 Beam（光束型）
05 Area（区域表现型）
06 Shield（护盾表现型）
07 Portal（传送门表现型）
08 Trail（拖尾型）
09 World（世界环境型）
10 Composite（复合型）

Projectile（视觉投射型）不是权威投射物；Shield（护盾表现型）不结算护盾数值；Area（区域表现型）不决定伤害范围；Portal（传送门表现型）不签发跨服票据。Composite（复合型）编排已有VFX定义，不另建第二套技能调度器。[U1：732—795]

以下十七类为内容制作／检索／任务／统计／人工核验分类，不对应插件或C++运行类：

01 Core（核心通用构件）
02 EnergyShield（能量、护盾与力场）
03 CastMagic（法术释放与魔法阵）
04 ProjectileBeam（投射物、光束与连接）
05 HitDamageExplosion（命中、伤害与爆炸）
06 AreaWarning（范围技能与地面预警）
07 StatusControl（持续伤害、状态与控制）
08 CharacterWeapon（角色、武器与动作增强）
09 Movement（移动、位移与穿行）
10 SummonTransform（召唤、变形、复活与死亡）
11 PortalSpacetime（传送门、裂隙与时空）
12 Environment（环境、天气、交互与破坏）
13 InteractionFeedback（战利品、目标与交互反馈）
14 UIScreen（用户界面、世界空间与屏幕空间）
15 UltimateComposite（终极技能与大型复合效果）
16 Experimental（高级模拟与实验）
17 Recovery（治疗、恢复、净化与复苏）

分类与行为正交：恢复类可以采用瞬时、附着、区域、光束或复合行为。保留原文 Core（核心通用构件）分类和 GamePlatformCore（平台核心插件）各自含义，不因名称相近混为同一模块。[U1：799—863]

内容所有权采用四类来源：平台中立默认、MOBA可选中立默认、项目默认、英雄／世界／皮肤内容包。它们是目录作用域，不是四层架构。基础层不存项目风格资源；公共VFX进入 DBAPresentationPack_Core（公共表现核心包）；专属效果归对应英雄或世界；皮肤包只覆盖表现，不写权威伤害和经济数据。

HeroId（英雄身份）、AbilityId（技能身份）、SkinId（皮肤身份）和 WorldContext（世界上下文）参与上下文匹配。它们必须来自合法上下文，不能靠用户提供任意路径突破权益、可见性或模块隔离。

9. 数据、目录、注册和租约

稳定语义使用 GameplayTag（玩法标签）；定义定位使用 PrimaryAssetId（主资产ID）；实际资源使用软引用。内容包身份、实例句柄、服务器ID与主资产ID不能混用。

GamePlatformData（平台数据）拥有统一发现、加载、版本、包依赖与调用者租约；VFX只分析自身需求并管理自己申请的租约，不自建全局加载器。[U1：1140—1183] 原生主资产和加载分组依托资产管理器；本项目目录及租约协议由工程实现，不是引擎自动附送的业务系统。[S05]

项目必需内容清单位于 Game/Content/Composition（项目组合资产），由流程组合节点／服务器任务装配入口选择；数据服务发现已安装已启用包，各领域服务注册目录并返回句柄。项目表现只协调表现部分，不能接管所有玩法内容的唯一管理权。

注册必须经过版本、目标用途、依赖无环和冲突预检；全部必需项成功后才发布本次解析视图。失败撤销本次已成功注册，释放本次租约，不遗留半启用状态。内容包无需为此增加C++模块；更不能把纯内容包的文件夹误当成自动注册动作。

解析的本版确定契约、退化规则和例子见规范P13。注销目录只停止未来选择，不得在现有表现实例仍使用资源时提前释放。实例、请求、预加载和注册句柄分别拥有职责；多世界共享加载需要由数据层合并所有仍有效租约。释放一个句柄不等于立刻完成垃圾回收或卸载全部资产。

10. 子系统、网络与权威动作

跨地图的流程、在线、会话、加载和数据门面按游戏实例生命周期管理；UI、输入和本地相机按本地玩家；世界、程序化生成协调和世界表现按当前世界。只在实际有服务职责时使用对应子系统，不能一插件一子系统。[S06]

VFX世界子系统放在客户端模块私有实现，选择可播放的客户端游戏世界；编辑器预览必须显式允许，烘焙命令行不得生成表现实例。全局静态单例、GWorld（全局世界指针）和固定第零号玩家不能替代作用域上下文。

服务器规则放游戏模式，公开复制状态放游戏状态／玩家状态，角色能力用组件，技能和属性使用GAS。游戏实例子系统／世界子系统不天然复制；网络事件必须指定复制通道、时效、重建和预测去重规则。持续护盾／光束状态不能仅依赖一次可能丢失的瞬时通知，新加入观察者需要从权威状态重建。

必要动作执行、取消、根运动或命中骨骼计算与纯表现动画分开。服务器可保留经审查确需的最小动作数据，但不因此加载纯VFX。低画质、特效缺失或迟到不能改变权威伤害；关键预警可选用低成本替代并标记诊断，不能把关键可读性预算简单降为零。

11. 模块目标与烘焙边界

共享模块可被客户端、服务器和编辑器消费；客户端模块仅在客户端／编辑器目标参与，服务器模块仅在服务器／编辑器目标参与；实际宿主类型、目标允许列表和加载阶段必须分别声明。ClientOnly（排除专用服务器的宿主）和 ServerOnly（排除专用客户端的宿主）的含义以引擎描述为准，名称不等同于完整目标隔离。[S03][S04]

VFX客户端模块必须允许编辑器读取其定义类型，所以不能用禁用所有Commandlet（命令行工具）的方式造成烘焙器无法反序列化源资产；同时，其世界服务必须在命令行和服务器世界不创建。服务器产品不链接VFX客户端模块，不加载纯表现目录、定义、Niagara资源或核验地图。编译、烘焙、Stage（暂存打包）、运行时加载分别审计，不以 Asset Bundle（资产加载分组）或目录名称当作剥离证据。

混合英雄包先保留单一源资产；按目标分别选择玩法／服务器／表现资产。无法安全剥离时再评审拆成同级玩法包和表现包，不能嵌套插件、复制权威定义或用文本占位UE资产。

原生Niagara池化和可伸缩能力优先使用；平台代码只负责策略映射和生命周期，不再实现按十类效果平行分裂的池。预算数值来自目标PC与移动设备实测，而不是把“GPU粒子”当作必然更快的结论。[S07]

12. 世界、PCG与地图制作

一期先完成常驻世界的真实三维入口、主城与最小野外活动区域，并同步接入服务器注册和准入。GamePlatformWorld（世界基础）拥有本实例上下文；全局分片分配仍在Go后端；GamePlatformPCG（程序化生成）消费世界上下文，不另存权威实例目录。

优先编辑器生成和审查后烘焙静态生态。客户端装饰性生成与影响碰撞、导航、采集、出生和胜负的结果分开；后者必须由权威执行或来自一致烘焙版本，不能只凭相同随机种子声称跨平台一致。

DBAWorldPack_OpenWorld（常驻世界包）包含地图源、世界／区域／体验定义、生成配置、数据层、环境资源与表现目录，大厅／主城／野外只是内部体验。新手村和主竞技场分别拥有对应包。外置Actor／对象编辑数据随地图完整提交，以引擎实际生成位置为准，不当作缓存清除。所有.uasset（资产）和.umap（地图）必须由引擎工具创建和迁移。

13. 共享协议与Go业务后端

跨UE与Go契约唯一真源在 Shared/Contracts（共享契约），分平台公共与项目扩展；后端内部服务／事件只在 Backend/internal/contracts（内部契约）。事件归属按真实消费者，不能一概把所有比赛或支付事件放到共享层。

C++生成源码位于 Shared/Generated/Cpp（C++生成目录），Go生成源码位于 Backend/generated（Go生成目录），分公共与项目输出但每种绑定只有一个来源。传输对象通过私有适配器转换，不进入公共玩法类型。需要C++绑定时先由统一构建编译外部库或采用已验证单一编译模块；只有头文件路径不代表实现已经链接。

保留五个Go薄应用入口：网关、身份、玩家数据、比赛、服务器控制。业务领域通过端口使用持久化、缓存和消息设施；领域不直接依赖第三方传输对象。后端当前公共平台／项目扩展内部布局以真实工程为准，此次插件文档不擅自重排Go结构。

预留 Commerce（交易）、Economy（经济账本）、Inventory（背包）、Entitlement（权益）、Progression（成长）和 LiveOps（运营）等职责。充值先确认可信渠道交易，再以幂等、可重试和可审计流程入账／发放；不能在角色资料直接加钱。已经落账的数据以冲正／补偿修正，不删除历史流水。可靠跨服务事件用事务发件箱和消费幂等，不宣称基础设施天然保证业务恰好一次。

比赛结果只接受绑定比赛的可信主竞技场服务器。一次性迁移票据绑定账号／角色、源或会话上下文、目标实例、版本、有效期和防重放身份；重连重新确认目标，不把旧大厅实例的票据换标签就复用。

14. 开发宿主、构建与两期交付

所有插件源码唯一位于 Game/Plugins（插件目录）；薄宿主只拥有启用列表、测试配置、测试地图和替身。源码不复制到宿主再分叉修改。使用既有获批宿主；第四个MOBA客户端宿主只有已存在／获批时实体化，不因本文件重复创建。

GamePlatformPresentationDev（平台表现宿主）用中立素材验证平台VFX，无MOBA／生肖依赖；GamePlatformRuntimeClientDev（通用运行宿主）关闭竞技验证世界、角色与准入；GamePlatformMobaClientDev（MOBA客户端宿主）和 GamePlatformMobaServerDev（MOBA服务器宿主）用中立数据验证比赛与表现语义；正式主工程最后组合项目内容。额外插件搜索路径不能代替模块启用、资产扫描和构建目标。

第一期包含三个里程碑，而不是再拆多期。

一期A：锁定核心、数据、流程、在线、会话、加载、世界、输入、角色、UI、相机、服务器控制及最小PCG工作流；一名英雄、一个常驻世界白盒跑通登录／创建／选择／准入／真实三维活动。基础日志、配置、资格检查及验证同步具备。

一期B：完成通用技能／战斗、必要动作、平台表现、VFX客户端与编辑器、项目上下文和首批真实内容。先实现可验收的瞬时／附着最小纵向链，再按实际技能扩展到附件十行为；尚未支持的行为必须明确拒绝，不能用空类或统一“成功”蒙混。进入一期结束验收前，当前产品实际使用的行为全部实测；未用到的高级复合、特定护盾或环境策略按同一框架增量实现。

一期C：接入竞技五模块和MOBA表现两个模块，统一验证五种人数、组队不拆分、可信准入、权威胜负、结果重试、结算、返回世界与重连。需要的新手村教学／训练采用同角色不同体验。十二生肖全套美术不作为第一条闭环的前置。

第二期深化生态、AI、导航、任务、背包、权益、成长、装备、充值商城、赛季、皮肤和运营工具；扩展独立本地化、设置和遥测。对第一期框架只做真实需要的扩展，不重新创造播放器、加载器、竞技框架或世界目录。热下载和功能热激活另行设计，不把普通插件默认当作Game Feature（动态游戏功能）。

15. 冲突迁移、验收与交付

首先将本版三份文档合入正确工作空间，保留旧文件的历史记录；再核查真实描述、构建、资产、协议及发布引用。改物理根目录不等于改插件挂载点。公共VFX包的旧名称存在真实引用时，先保留并提供迁移映射、重定向和回退计划；不要删文件后声称“目录已统一”。

必须分别验证：插件／模块依赖无环；反射类与目标可见；编辑器、独立客户端、独立服务器构建；两目标干净烘焙和暂存；多实例资源租约；目录歧义和注册回滚；复合循环与取消；预测去重；迟到回调不复活实例；VFX人工核验；服务器无纯表现执行；世界准入和五模式联调。详细验收门禁见插件规范P24—P27。

本次仅提供文档和规划索引，索引验证范围只覆盖声明的设计关系，不能当作完整真实模块图。源码、UE资产、后端接口、数据库、部署和生产环境均未因本次文档生成自动修改。

### 15.1 全局工程代码命名与中文说明

整个 DivineBeastsWorkspace 的文件、目录、插件、模块、类型、接口、函数、变量、配置、协议字段、测试和脚本，必须使用准确、可检索且符合所在语言、引擎、生成器标准惯例的英文技术标识；不得使用拼音、含义不明缩写、误导性历史名称或没有明确职责的泛化命名。名称要能表达真实职责、层级、端侧、作用域、生命周期和必要单位：动作使用动词，布尔值可判断，集合使用复数，数值标注时间、大小、数量、最小货币单位或版本等含义。UE 反射类型、模块构建规则、注册名和导出宏保持一致；Go、Proto、OpenAPI、JSON Schema 与配置遵循各自既有兼容规则，禁止为表面统一破坏已发布身份。

对外 API、跨模块接口、反射类型、协议与配置字段、业务规则、异步／并发回调、资源所有权、线程和生命周期、网络权威边界、迁移、构建部署脚本及非显然测试，必须有与实现同步的中文说明。说明解释职责、前提、输入输出、错误／取消语义及必要的权限、线程、所有权或兼容性边界，而非逐行复述代码。中文 README、目录规划、架构、接口、配置和测试说明在命名或职责变动时同时维护。自动生成或第三方代码不手工加注释，其来源、版本、生成方式和消费边界写在契约真源、生成配置或相邻中文文档中。完整可执行规则以 `AGENTS.md` 的“全局工程代码命名、中文注释与说明规范”为准。

参考依据与技术验证边界

[U1] 用户附件《神兽联盟》三层架构 VFX（视觉特效）最终目录设计，1547行；本包保存原文于 Docs/References/VFXDesignSource.md（VFX来源原文）。文中原有扩展目录示例与既定五模块的差异，按本版“整合决定”逐项标明，不把整合决定冒充附件原文。

[B1] 本会话已交付的v1.2.0总体规划、插件目录规划、插件开发规范与工程规则，以及用户随后明确的根目录更名。本版只改变明确列出的设计归属，不声明真实工程已经迁移。

以下官方资料仅核对引擎／工具机制，不规定本项目的三层名称、目录或业务规则。核对日期：2026-09-21；实际实现以锁定UE5.8补丁源码及真实构建为准。

[S01] Epic，Plugins（插件发现、多模块与内容插件）。
https://dev.epicgames.com/documentation/en-us/unreal-engine/plugins-in-unreal-engine

[S02] Epic，Unreal Engine Modules（模块和依赖）。
https://dev.epicgames.com/documentation/en-us/unreal-engine/unreal-engine-modules

[S03] Epic，EHostType::Type（宿主类型）。
https://dev.epicgames.com/documentation/en-us/unreal-engine/API/Runtime/Projects/EHostType__Type

[S04] Epic，FModuleDescriptor（模块目标限制）。
https://dev.epicgames.com/documentation/en-us/unreal-engine/API/Runtime/Projects/FModuleDescriptor

[S05] Epic，Asset Management（主资产与加载）。
https://dev.epicgames.com/documentation/en-us/unreal-engine/asset-management-in-unreal-engine

[S06] Epic，Programming Subsystems（子系统生命周期）。
https://dev.epicgames.com/documentation/en-us/unreal-engine/programming-subsystems-in-unreal-engine

[S07] Epic，Scalability and Best Practices for Niagara（粒子特效伸缩与池化）。
https://dev.epicgames.com/documentation/en-us/unreal-engine/scalability-and-best-practices-for-niagara

[S08] OpenAI，Custom instructions with AGENTS.md（工程规则发现和体积限制）。
https://developers.openai.com/codex/guides/agents-md

本次不是UE工程实现、模块编译、资源烘焙或服务器联调；以上资料不构成这些验证已经通过的证据。
