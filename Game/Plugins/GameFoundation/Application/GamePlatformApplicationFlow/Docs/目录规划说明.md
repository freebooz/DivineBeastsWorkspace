# GamePlatformApplicationFlow 目录规划说明

版本：0.1.0｜日期：2026-09-21。正式源码仅维护下列插件位置，不复制到宿主或 Shared。

```text
Game/Plugins/GameFoundation/Application/GamePlatformApplicationFlow/ # 通用应用流程执行机制
├── GamePlatformApplicationFlow.uplugin                              # 插件身份、目标与模块声明
├── README.md                                                        # 中文接入入口与验收边界
├── Config/                                                          # 打包工具配置，不是运行时配置
│   └── FilterPlugin.ini                                             # 独立打包携带中文文档和验证入口
├── Source/                                                          # 唯一工程源码
│   └── GamePlatformApplicationFlow/                                 # Runtime 模块
│       ├── GamePlatformApplicationFlow.Build.cs                      # 模块构建和依赖规则
│       ├── Public/                                                  # 跨模块公开契约
│       │   ├── API/                                                 # 服务入口
│       │   │   └── GamePlatformApplicationFlowSubsystem.h            # GameInstance 流程子系统
│       │   ├── Definitions/                                         # M0中立资产定义
│       │   │   └── GamePlatformFlowDefinition.h                      # 反射节点、输入身份、路由与显式循环策略
│       │   ├── Interfaces/                                          # 组合根注入契约
│       │   │   ├── GamePlatformFlowNode.h                            # 抽象节点执行与清理
│       │   │   └── GamePlatformCallbackFlowNode.h                    # 配对回调注入节点
│       │   └── Types/                                               # 中立值类型
│       │       └── GamePlatformFlowTypes.h                           # 句柄、上下文、图、结果与快照
│       └── Private/                                                 # 不向业务公开的实现
│           ├── GamePlatformApplicationFlowModule.cpp                 # 默认模块注册
│           ├── Definitions/                                         # 资产与唯一执行器的转换
│           │   ├── GamePlatformFlowDefinition.cpp                    # Data基类与流程图校验
│           │   └── GamePlatformFlowDefinitionConversion.h             # 私有无节点图转换契约
│           ├── Execution/                                           # 标准 C++ 生产调度核心
│           │   ├── ApplicationFlowExecutor.h                        # 状态机、邮箱与内部契约
│           │   └── ApplicationFlowExecutor.cpp                      # 校验、推进、重试、取消与清理
│           ├── Services/                                            # 节点服务实现
│           │   └── GamePlatformCallbackFlowNode.cpp                  # 绑定验证及回调分发
│           ├── Subsystems/                                          # UE 生命周期适配
│           │   └── GamePlatformApplicationFlowSubsystem.cpp          # UObject 保活、作用域与 Ticker
│           └── Tests/                                               # 仅开发测试代码与测试替身
│               ├── ApplicationFlowExecutorCases.h                   # 两个测试入口共用的核心行为场景
│               ├── ApplicationFlowAutomationTests.cpp               # UE 核心自动化入口
│               ├── ApplicationFlowSubsystemTests.cpp                # UE 作用域与关闭测试
│               ├── ApplicationFlowAssetTests.cpp                    # UE资产结构、工厂和事件令牌测试
│               └── ApplicationFlowLeaseIntegrationTests.cpp         # 真实Data/Flow资产租约潜伏测试
├── Tests/                                                           # 可重复验证工具
│   ├── CMakeLists.txt                                               # 独立编译同一生产核心，不复制实现
│   ├── Standalone/                                                  # 原生测试入口
│   │   └── ApplicationFlowTests.cpp                                 # 逐场景运行并返回失败退出码
│   └── Scripts/                                                     # 中文说明的验证脚本
│       ├── TestApplicationFlow.ps1                                  # 原生 Debug／Release 编译与测试
│       └── TestApplicationFlowUnreal.ps1                            # 真实 UE5.8 宿主构建与报告检查
└── Docs/                                                            # 中文工程说明
    ├── 架构与生命周期说明.md                                        # 边界、状态、线程和对象所有权
    ├── 接口与接入说明.md                                            # API、错误及组合根装配示例
    ├── 目录规划说明.md                                              # 本目录真源与维护约束
    ├── 测试与验证说明.md                                            # M0前历史命令、覆盖和证据
    └── M0兼容扩展与验证.md                                          # 本轮准确签名、兼容边界和验证证据
```

新增、移动、重命名或删除文件时，同步维护本文、插件 README 和解决方案《总体目录规划说明》。改变契约时同步修改接口说明及相应用例。规范文档使用中文文件名，引擎、构建工具和脚本标识遵循技术命名约定。

本插件提供资产类型，具体定义由主工程通过引擎生成，不创建Content或Editor空壳。Config仅包含打包文件过滤规则，不注册运行配置或标签。登录、选角、世界准入等节点归领域插件或项目组合根。

原生脚本默认输出工作空间 `Saved/Validation/ApplicationFlow/Native/`；本次用BuildDirectory显式写入 `Saved/Validation/FoundationM0/FlowNative/`，包括独立保存的红、绿测试日志，均不属于源码。UE报告须使用全新目录，不以历史报告替代本次结果。

UE 构建会自动生成插件 `Intermediate/`（UHT 与编译缓存）及成功时的 `Binaries/`（模块二进制），均为可再生成产物，不逐文件列入上方正式源码树。临时验证宿主位于工作空间 `Saved/Validation/ApplicationFlow/UEHost/`，通过 AdditionalPluginDirectories 引用本目录，不作为正式游戏主工程或第二套插件源码。
